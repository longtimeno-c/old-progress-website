{
  "name": "progress-website",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Files already in dist/'",
    "vercel-build": "echo 'No build step needed'"
  },
  "dependencies": {
    "googleapis": "^105.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}